{#
    By default, forms enable client-side validation. This means that you can't
    test the server-side validation errors from the browser. To temporarily
    disable this validation, add the 'novalidate' attribute:

    {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
#}

{% if show_confirmation|default(false) %}
    {% set attr = {'data-confirmation': 'true'} %}
    {{ include('app/_delete_ticket_confirmation.html.twig') }}
{% endif %}

{{ form_start(form, {attr: attr|default({})}) }}
    {{ form_widget(form) }}

    <button type="submit" class="{{ button_css|default("btn btn-primary") }}">
        <twig:ux:icon name="tabler:save-changes"/> Enregistrer le ticket
    </button>

    {% if include_back_to_home_link|default(false) %}
        <a href="{{ path('app_tickets_list') }}" id="bachToList_link">
            <twig:ux:icon name="tabler:list"/>Annuler et retourner à la liste
        </a>
    {% endif %}
{{ form_end(form) }}